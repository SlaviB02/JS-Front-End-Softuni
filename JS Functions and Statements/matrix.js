function printMatrix(number)
{
    let row=[];
    for(let j=0;j<number;j++)
        {
row.push(number);
        }
    for(let i=0;i<number;i++)
    {
        console.log(row.join(" "));
    }
}
